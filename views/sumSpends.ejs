<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sum Spends</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Custom color scheme */
        :root {
            --primary-color: #ff6a00;
            /* Primary app color */
            --secondary-color: #6c757d;
            /* Secondary color */
            --background-color: #f8f9fa;
            /* Background color */
            --text-color: #343a40;
            /* Text color */
            --button-color: #007bff;
            /* Button color */
            --button-hover-color: #0056b3;
            /* Button hover color */
            --orange-color: #ff8000;
            /* Orange color for custom buttons */

        }

        body {
            background-color: var(--background-color);
            color: var(--text-color);
            font-family: Arial, sans-serif;
        }

        .form-container {
            max-width: 500px;
            margin: 50px auto;
            padding: 20px;
            background-color: white;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .form-label {
            color: var(--text-color);
        }

        .btn-primary {
            background-color: var(--button-color);
            border-color: var(--button-color);
        }

        .btn-orange {
            background-color: var(--orange-color);
            border-color: var(--orange-color);
            border-radius: 10px;
            padding: 12px;
            font-size: 1rem;
            transition: background-color 0.3s ease, transform 0.2s ease;
        }

        .btn-orange:hover {
            background-color: #f95b00;
            transform: translateY(-2px);
        }

        .btn-primary:hover {
            background-color: var(--button-hover-color);
            border-color: var(--button-hover-color);
        }

        .result-box {
            margin-top: 20px;
            padding: 15px;
            background-color: #ffcc00;
            /* New background color */
            color: #ffffff;
            /* Text color */
            border-radius: 5px;
            text-align: center;
            font-weight: bold;
            box-shadow: 0 0 8px rgba(0, 0, 0, 0.1);
            width: 100%;
            /* Full width */
            max-width: 500px;
            /* Max width to keep it centered */
            transition: transform 0.3s ease-in-out;
        }

        .result-box:hover {
            transform: scale(1.02);
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="form-container">
            <h2 class="text-center mb-4" style="color: var(--primary-color);">Calculate Spends</h2>
            <form action="/calculateSpends" method="POST">
                <input class="idInp" type="hidden" id="userId" name="userId" value="<%= userId %>" required>

                <div class="mb-3">
                    <label for="year" class="form-label">Year:</label>
                    <input type="text" class="form-control" id="year" name="year" placeholder="YYYY" required>
                </div>

                <div class="mb-3">
                    <label for="month" class="form-label">Month:</label>
                    <input type="text" class="form-control" id="month" name="month" placeholder="MM" required>
                </div>

                <div class="d-grid">
                    <button type="submit" class="btn btn-primary">Calculate</button>
                </div>
            </form>
        </div>
    </div>

    <% if (typeof totalSpends !=='undefined' ) { %>
        <div class="result-box mx-auto">
            <h4>Total Spends for <%= `${year}-${month}` %>: ₹ <%= totalSpends %>
            </h4>
        </div>
        <% } %>

            <div class="d-flex justify-content-center mt-4 ">
                <a href="/home" class="btn btn-orange mx-2">Back To Home</a>
            </div>



            <!-- Bootstrap JS (optional) -->
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>